# TODO

- [ ] Add GST input UX: optional field with format validation (auto-uppercase, remove invalid chars)
- [ ] Show validation error message only when user has typed something
- [ ] Prevent submit when GST format invalid
- [ ] Verify build/lint passes
