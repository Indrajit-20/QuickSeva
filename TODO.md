# TODO

- [ ] Fix Navbar search URL to use `?category=` instead of `?q=`
- [ ] Fix ServicesPage to read `category` and fallback to `q` (prefer `category`)
- [ ] Make category matching case-insensitive + partial include behavior
- [ ] Update active chip logic + page title rules for matched/typed/none
- [ ] Run frontend sanity check
