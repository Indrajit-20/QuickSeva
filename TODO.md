# TODO - QuickSeva Fixes

- [ ] Inspect current `Home.jsx` and remove `UrbanServiceCategories` import + component usage only.
- [ ] Update `Hero.jsx` service icon grid to use unique Lucide icons per service.
- [ ] Verify `lucide-react` is available; if not, add installation step (no code changes unless confirmed).
- [ ] Run frontend lint/build (or dev server smoke test) to ensure no broken imports.
